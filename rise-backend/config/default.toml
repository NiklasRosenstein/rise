[server]
host = "0.0.0.0"
port = 3000
public_url = "http://localhost:3000"

[auth]
secret = "super-secret-key-for-development"

[pocketbase]
url = "http://pocketbase:8090"
service_email = "rise@rise-backend.svc.local"
service_password = "change-me-in-production"

# Container registry configuration (optional)
# Uncomment and configure ONE of the following registry providers:

# AWS ECR Example:
# [registry]
# type = "ecr"
# region = "us-east-1"
# account_id = "123456789012"
# # Optional: Provide AWS credentials (if not using IAM role)
# # access_key_id = "AKIAIOSFODNN7EXAMPLE"
# # secret_access_key = "wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY"

# Generic Docker Registry Example (assumes user is authenticated via 'docker login'):
[registry]
type = "docker"
registry_url = "localhost:5000"
namespace = "rise-apps"

# Docker Hub Example:
# [registry]
# type = "docker"
# registry_url = "docker.io"
# namespace = "myorg"