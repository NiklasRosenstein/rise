[server]
host = "0.0.0.0"
port = 3000
public_url = "http://localhost:3000"

[auth]
issuer = "http://localhost:5556/dex"
client_id = "rise-backend"
client_secret = "rise-backend-secret"
# List of admin user emails (have full permissions on all resources)
admin_users = ["admin@example.com"]

[database]
url = "postgres://rise:rise123@localhost:5432/rise"

# Container registry configuration (optional)
# Uncomment and configure ONE of the following registry providers:

# AWS ECR Example:
# [registry]
# type = "ecr"
# region = "us-east-1"
# account_id = "123456789012"
# repository = "rise-apps"  # Required: ECR repository name
# push_role_arn = "arn:aws:iam::123456789012:role/rise-ecr-push"  # Required: IAM role for scoped credentials
# # prefix = "production"  # Optional: prefix within repository
# # access_key_id = "AKIAIOSFODNN7EXAMPLE"  # Optional: static credentials (use IAM role in production)
# # secret_access_key = "wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY"
# # Image path: {account}.dkr.ecr.{region}.amazonaws.com/{repository}/{prefix}/{project}:{tag}

# OCI registry for local development (client-side auth via docker login)
[registry]
type = "oci-client-auth"
registry_url = "localhost:5000"
namespace = "rise-apps"

# Docker Hub Example (using oci-client-auth):
# [registry]
# type = "oci-client-auth"
# registry_url = "docker.io"
# namespace = "myorg"