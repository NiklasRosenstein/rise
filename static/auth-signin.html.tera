<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign In - Rise</title>
    <link rel="stylesheet" href="/css/pico.min.css">
    <link rel="stylesheet" href="/css/app.css">
    <style>
        .private-badge {
            display: inline-block;
            background: var(--primary);
            color: var(--primary-inverse);
            padding: 0.25rem 0.75rem;
            border-radius: 0.25rem;
            font-size: 0.875rem;
            font-weight: 600;
            margin-bottom: 1rem;
        }
        .project-name {
            font-family: monospace;
            background: var(--code-background-color);
            padding: 0.125rem 0.375rem;
            border-radius: 0.25rem;
        }
        #signin-card {
            max-width: 500px;
            margin: 2rem auto;
        }
        .info-text {
            color: var(--muted-color);
            margin: 1rem 0;
        }
    </style>
</head>
<body>
    <main class="container">
        <article id="signin-card">
            <header>
                <h1>Rise</h1>
                <span class="private-badge">🔒 Private Project</span>
                <h2>Authentication Required</h2>
            </header>
            <section>
                <p>
                    The project <strong class="project-name">{{ project_name }}</strong> is private
                    and requires authentication to access.
                </p>
                <p class="info-text">
                    You'll be redirected to sign in with your identity provider.
                    After authentication, you'll be able to access this project.
                </p>
                <a href="{{ continue_url }}" role="button">Sign In</a>
                {% if redirect_url %}
                <p class="info-text">
                    <small>After signing in, you'll be redirected to: <br><code>{{ redirect_url }}</code></small>
                </p>
                {% endif %}
            </section>
        </article>
    </main>
</body>
</html>
