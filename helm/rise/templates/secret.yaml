# This template is intentionally empty.
# Users must provide an existingSecret with the following keys:
#   - auth-client-secret: OAuth client secret
#   - database-password: PostgreSQL password
#   - database-url: Full database URL (e.g., postgres://rise:password@postgresql:5432/rise)
#
# Example:
# kubectl create secret generic rise-secrets \
#   --from-literal=auth-client-secret=YOUR_CLIENT_SECRET \
#   --from-literal=database-password=YOUR_DB_PASSWORD \
#   --from-literal=database-url=postgres://rise:YOUR_DB_PASSWORD@postgresql:5432/rise
